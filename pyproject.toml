[project]
name = "morning-news"
version = "0.1.0"
description = "Morning news RSS fetcher and digest"
requires-python = ">=3.11"
dependencies = [
    "feedparser>=6.0.0",
    "requests>=2.31.0",
    "pyyaml>=6.0",
    "langgraph>=1.2.1",
    "openai>=1.0.0",
]

[dependency-groups]
dev = []
